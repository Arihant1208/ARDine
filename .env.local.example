# Backend API Configuration
PORT=4000
CORS_ORIGIN=http://localhost:3000

# Gemini AI API Key (server-side only)
GEMINI_API_KEY=your_gemini_api_key_here

# Back-compat: backend also checks API_KEY
API_KEY=your_gemini_api_key_here

# Optional: used ONLY by `npm run db:setup` (backend/db/setup.ts)
# The running app currently uses the demo/in-memory DB in database/dbClient.ts.
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.ofcyuvkiirrbkwrotywy.supabase.co:5432/postgres

# Frontend API Base URL (for production).
# In local dev, Vite proxies `/api` to http://localhost:4000 by default.
VITE_API_BASE_URL=http://localhost:4000
